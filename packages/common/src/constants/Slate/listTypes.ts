import BLOCKS from "./blocks"

/**
 * A list of all list types
 * (I might want to add for example todo or toggle lists in the future)
 */

const LIST_TYPES = [BLOCKS.LIST_NUMBERED, BLOCKS.LIST_BULLETED]

export default LIST_TYPES
